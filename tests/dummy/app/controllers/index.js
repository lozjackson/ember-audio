import Controller from '@ember/controller';

// var alias = Ember.computed.alias;

export default Controller.extend({

  // bus: alias('audioService.busses.firstObject'),
  // gainNode: null,
  // gainNode: alias('bus.nodes.firstObject.node'),

  // init() {
  //   this._super(...arguments);
  //   // var audioService = this.get('audioService');
  //   var audioService = this.audioService;
  //   var output = audioService.get('output');
  //   var gain = audioService.createGain();
  //   //audioService.get('busses').objectAt(0).connect(gain);
  //   //gain.connect(output, );
  //   gain.set( 'input', audioService.get('busses').objectAt(0).bus );
  //   gain.set('output', output);
  //
  //   this.set('gainNode', gain);
  // }
});
